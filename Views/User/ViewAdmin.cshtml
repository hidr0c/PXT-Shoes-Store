@model VinaShoseShop.Models.Nguoidung
@{
    ViewBag.Title = "Test";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var u = Session["use"] as VinaShoseShop.Models.Nguoidung;
}

@{
    if (u != null)
    {
        <h2>Xin chào: @u.Email</h2>
        @Html.ActionLink("Đăng xuất", "Index", "Home")
        @Html.ActionLink("Quản lý người dùng", "Index", "Home")
    }
}